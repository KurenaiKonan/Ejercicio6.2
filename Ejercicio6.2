class Alumno :
    _nombre = None
    _nota = 0
    _nota_minima = 5

    def __init__( self, nombre, nota ) :
        self._nombre = nombre
        self._nota = nota

    def resultado( self ) :
        return f'{ self._nombre } con una nota de { self._nota } { "ha aprobado" if self._nota >= self._nota_minima else f"NO aprobó, la nota minima es de { self._nota_minima }" } '

Jace = Alumno( 'Jace', 2 )
print( jace.resultado() )

Liliana = Alumno( 'Liliana', 9 )
print( liliana.resultado() )
